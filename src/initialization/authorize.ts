import authorizeGeneratorOptions from '../../bookmakers/template/initialization/authorizeGeneratorOptions';
import authorizeGenerator from '../../utils/generators/initialization/authorizeGenerator';

const authorize = authorizeGenerator(authorizeGeneratorOptions);

export default authorize;
