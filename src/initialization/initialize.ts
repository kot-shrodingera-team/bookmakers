import initializeGeneratorOptions from '../../bookmakers/template/initialization/initializeGeneratorOptions';
import initializeGenerator from '../../utils/generators/initialization/initializeGenerator';

const initialize = initializeGenerator(initializeGeneratorOptions);

export default initialize;
