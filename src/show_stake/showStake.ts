import showStakeGeneratorOptions from '../../bookmakers/template/show_stake/showStakeGeneratorOptions';
import showStakeGenerator from '../../utils/generators/show_stake/showStakeGenerator';

const showStake = showStakeGenerator(showStakeGeneratorOptions);

export default showStake;
